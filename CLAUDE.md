# Claude instructions

Follow `AGENTS.md` and the public specification. Keep filesystem mutation in the Rust core, preserve the safety invariants, and run `npm run check` before proposing completion.
